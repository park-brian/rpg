interface:
  display_name: "Code Simplifier"
  short_description: "Behavior-preserving code cleanup"
  default_prompt: "Use $code-simplifier to simplify the code I just changed without altering behavior."
